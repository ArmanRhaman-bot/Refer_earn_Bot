/*CMD
  command: /iv
  help: 
  need_reply: false
  auto_retry_time: 
  folder: Start-Menu

  <<ANSWER

  ANSWER

  <<KEYBOARD

  KEYBOARD
  aliases: 
  group: 
CMD*/

Bot.sendMessage( inspect(user) )
Bot.sendMessage( inspect(chat) )
Bot.sendMessage(inspect(options))
